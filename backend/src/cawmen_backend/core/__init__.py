"""Pure functional core: chase mechanics and seed-driven Case generation.

No I/O, no mutation of external state, no language — transition functions take data in
and return new data plus structured, language-free facts (see ADR-0008).
"""
